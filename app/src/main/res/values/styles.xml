<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#0288D1</item>
        <item name="android:statusBarColor">@color/topBar</item>

        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@color/topBar</item>
        <item name="colorControlHighlight">@color/topBar</item>
    </style>

    <style name="AppBarOverlay" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="PopupOverlay" parent="Theme.AppCompat.Light.NoActionBar" />

</resources>
